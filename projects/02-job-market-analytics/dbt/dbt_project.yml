name: job_market_analytics
version: 1.0.0
config-version: 2

profile: job_market_analytics

model-paths:
  - models
test-paths:
  - tests

models:
  job_market_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
