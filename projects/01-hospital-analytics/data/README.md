# Data Layers

This directory provides local placeholders for medallion-aligned project data assets.

- `bronze/`: raw landed data
- `silver/`: cleaned and standardized data
- `gold/`: curated analytical outputs

No sample datasets are included at this stage.
