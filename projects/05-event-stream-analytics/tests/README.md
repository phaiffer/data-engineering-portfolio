# Tests

This project keeps tests focused on stable, high-value logic:

- checkpoint file behavior and bounded-run stop conditions;
- partition path generation;
- pure Gold helper logic.

The test suite intentionally avoids depending on the live public stream or a running broker.
