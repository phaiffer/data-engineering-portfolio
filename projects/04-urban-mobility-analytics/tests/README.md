# Tests

This project keeps tests focused on stable, high-value logic:

- month planning and manifest tracking;
- partition path generation;
- pure Gold helper logic.

The test suite intentionally avoids downloading the public dataset during test execution.
